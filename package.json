{
  "name": "github-pr-review-helper",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that reduces GitHub PR review noise by hiding low-signal files and summarizing risky changes.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.0",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3"
  }
}
